import Title from './Title';

const Header = () => {
  return (
    <header className="fixed w-full p-2 h-12 bg-white dark:bg-gray-800 z-[999]">
      <Title />
    </header>
  );
};

export default Header;
